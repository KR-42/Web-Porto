import Image from "next/image";import Link from "next/link";import { ArrowUpRight } from "lucide-react";import type { Project } from "@/data/projects";import { TagList } from "./ui";
export function ProjectCard({project,index}:{project:Project;index:number}){return <article className="project-card"><Link href={`/projects/${project.slug}`} className="project-image"><Image src={project.images[0].src} alt={project.images[0].alt} fill sizes="(max-width: 800px) 100vw, 50vw"/><span>0{index+1}</span></Link><div className="project-body"><div className="meta"><span>{project.category}</span><time>{project.year}</time></div><h3><Link href={`/projects/${project.slug}`}>{project.shortTitle}</Link></h3><p>{project.summary}</p><TagList items={project.technologies.slice(0,4)}/><Link className="card-link" href={`/projects/${project.slug}`}>View case study <ArrowUpRight/></Link></div></article>}
